{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "source ROS workspace",
            "type": "shell",
            "command": "source /opt/ros/melodic/setup.bash && export ROS_MASTER_URI=http://localhost:11311/",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}